# DeepInfra API key used by minuet-ai.nvim for code completion.
# Copy this file to `.env`, fill in your token, and source it before launching nvim:
#   export $(grep -v '^#' .env | xargs)
DEEPINFRA_API_KEY=TOKEN
